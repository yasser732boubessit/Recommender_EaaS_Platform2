server:
  port: 9004

spring:
  application:
    name: apigateway

  cloud:
    gateway:
      globalcors:
        add-to-simple-url-handler-mapping: true
        corsConfigurations:
          '[/**]':
            allowedOrigins:
              - "http://localhost:3000"
            allowedMethods:
              - GET
              - POST
              - PUT
              - DELETE
              - OPTIONS
            allowedHeaders:
              - "*"
            allowCredentials: true

      routes:
        - id: reply-service
          uri: lb://reply-service
          predicates:
            - Path=/reply/**
        - id: manager
          uri: http://localhost:8049
          predicates:
            - Path=/interaction/**

eureka:
  client:
    service-url:
      defaultZone: http://localhost:9107/eureka/
    register-with-eureka: true
    fetch-registry: true

logging:
  level:
    org.springframework.cloud.gateway: TRACE
    reactor.netty.http.client: DEBUG
    reactor.netty.http.server: DEBUG
